<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.min.css" />
    <link rel="stylesheet" href="/css/main.css">
    <title>Management message</title>
</head>

<body>



    <%- include('./part/aside'); %>
    <main class="page_content mt-5 mt-md-0 px-3">
        <div class="row justify-content-center mt-3">
                <div class="col-md-6">
                    <div class="card rounded-5 shadow-sm p-3">
                        <h5 class="mb-4 text-capitalize">add logo to service</h5>
                        <form action="/dashbord/services/logo" method="post" enctype="multipart/form-data">
                            <input type="text" name="serviceId" value="<%= service._id %>" hidden>
                            <div class="mb-3">
                                <label for="formFile" class="form-label">logo image</label>
                                <input class="form-control" name="image" type="file" id="formFile">
                            </div>
                            <div class="mb-3">
                                <label class="form-label label-1 text-capitalize">link logo</label>
                                <input type="url" class="form-control" name="link" placeholder="enter link logo"
                                    required>
                            </div>
                            <button class="btn btn-primary" type="submit">submit</button>
                        </form>
                    </div>
                </div>
            </div>
            <div class="container mt-5">
                <div class="row g-3">
                    <% for(let logo of service.logos) { %>
                        <div class="col-6 col-sm-4 col-md-3 position-relative">
                            <a href="<%= logo.link%>" class="stretched-link"></a>
                            <img src="/<%= logo.image%>" alt="">
                        </div>
                    <% } %>


                </div>
            </div>
        </main>






        <script src="/js/popper.min.js"></script>
        <script src="/js/bootstrap.min.js"></script>
</body>

</html>